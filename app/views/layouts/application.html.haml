!!!
%html
  %head
    = render 'layouts/head_config'
    %title Quick shipping QMON
  %body
    .container
      - flash.each do |name, msg|
        = content_tag :div, msg, class: "alert alert-#{name}"
      = yield
